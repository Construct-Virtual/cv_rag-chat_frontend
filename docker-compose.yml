version: "3.8"

services:
  # PostgreSQL with pgvector extension
  # postgres:
  #   image: pgvector/pgvector:pg16
  #   container_name: cv_rag_postgres
  #   environment:
  #     POSTGRES_DB: cv_rag_chat
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres_password_change_me
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres -d cv_rag_chat"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   restart: unless-stopped

  # Backend FastAPI (optional - can also run locally with uvicorn)
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: cv_rag_backend
    # environment:
    #   - DATABASE_URL=${DATABASE_URL}
    #   - OPENAI_API_KEY=${OPENAI_API_KEY}
    #   - JWT_SECRET_KEY=${JWT_SECRET_KEY:-dev_secret_key_for_testing_only_change_in_production_12345678}
    #   - JWT_ALGORITHM=HS256
    #   - JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
    #   - JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
    #   - CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
    #   - DEBUG=true
    env_file:
      - ./.env
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped

# volumes:
#   postgres_data:
#     driver: local
